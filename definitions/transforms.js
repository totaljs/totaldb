/*

NEWTRANSFORM('data_insert_before', function($, value) {
	$.value = value.form;
	$.next();
});;

NEWTRANSFORM('data_insert_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_update_before', function($, value) {
	$.value = value.form;
	$.next();
})

NEWTRANSFORM('data_update_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_read_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_read_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_find_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_find_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_list_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_list_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_remove_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('data_remove_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_find_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_find_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_min_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_min_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_max_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_max_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_sum_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_sum_after', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_count_before', function($, value) {
	$.value = value;
	$.next();
});

NEWTRANSFORM('types_count_after', function($, value) {
	$.value = value;
	$.next();
});

*/